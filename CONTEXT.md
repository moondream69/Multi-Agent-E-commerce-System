# Multi-Agent E-commerce System

> **本文件是重构目标态的术语表**(2026-09-08 五轮访谈定稿,见 `docs/adr/0005`)。当前代码实现为旧系统(冻结中),与此术语表存在差异;实现以目标态术语为准,旧系统术语仅在其被取代的决策记录(ADR-0001~0004)中保留。

## 定位

内部卖家工具(2-5 人跨境卖家团队、局域网部署),真实生产定位。买家侧为模拟数据对象(seed 买家 + simulator 演练流量),不接真实买家渠道;系统服务卖家运营,覆盖选品分析、订单管理、客户服务三域。

## 运行实体

**经理 Agent**(Manager):
supervisor 模式的顶层 Agent:规划拆分用户需求为切片任务、分派给业务 Agent、协调信息传递、汇总结果。零写权限 + 只读查询(指挥不亲自动手)。前端以第 4 角色可见,规划/分派轨迹透明。
_Avoid_: 调度器、编排器(那是机制不是角色)

**业务 Agent**:
承担一类业务任务的执行单元,共三个:选品分析(ProductResearch)、订单管理(OrderManagement)、客服(CustomerService,定位为多语言起草助手)。每个 Agent 是一个 LangGraph 子图,工具按节点动态暴露。
_Avoid_: 机器人、助手、智能体机器人

**工具**(Tool):
供 Agent 声明、由 LLM 调用的最小可执行功能。工具暴露遵循风险三层分类:免审/审批/禁做。
_Avoid_: 能力、插件

**任务**(Task):
一次用户请求的完整执行单元,由 Manager 规划为若干切片;一个任务 = 一个执行线索(thread)。
_Avoid_: 请求(避免与 HTTP 请求混淆)

**切片**(Slice):
任务中由人工断点分隔的执行段。Manager 规划时按人工环节切分;切片执行完毕 → interrupt 挂起 → 审批决定 → 用户指令驱动下一段;切片被拒 → Manager 携拒绝原因重规划。
_Avoid_: 阶段(与旧客服工作流的历史语义混淆)、步骤

**执行线索**(thread):
LangGraph 的一次持久执行单元(thread_id),状态经 checkpointer(PostgresSaver)落库,可挂起/恢复/跨重启。粒度 = 一次用户请求。
_Avoid_: 会话、任务(层次不同:任务=业务单元,thread=执行单元)

**会话**(Session):
一次连续对话的容器,归属登录用户;多会话 UI 与历史隔离保留(新建/切换/删除、登录新开、刷新恢复)。LLM 上下文 = 短上下文(最近 N 轮)+ 超窗摘要;不做长期记忆。
_Avoid_: 对话、聊天(泛)

**商品草稿**:
选品报告完成后自动创建的商品(draft 状态,未上架)。LLM 从报告提炼字段,提炼失败降级为报告标题 + 默认价。draft 内部编辑免审,上架(对外状态变更)进审批。
_Avoid_: 自动上架(草稿≠上架)

**库存告警阈值**:
商品级字段(默认 10):库存扣减后低于该值触发五档库存告警通知;经草稿工具或商品 CSV 可选列设置。
_Avoid_: 全局安全线(阈值按商品设置)

**回复模板**(ReplyTemplate):
客服话术的标准文本,含变量占位(如 {order_id}),按 场景+语言 唯一。
_Avoid_: 话术库、快捷回复

**工单**(Ticket):
客服升级人工后的实体:escalate 落表,审批中心/工作台可见列表。升级不再是断头事件。
_Avoid_: 升级记录、投诉单

**演示买家**:
模拟买家数据的代表(seed 客户「张伟」),演练环境驱动闭环演示;生产环境不跑模拟流量。
_Avoid_: 用户、账户

## 编排

**监督图**(supervisor graph):
Manager + 三个业务子图组成的顶层状态图:规划 → 分派(独立并行/依赖串行)→ 汇总。
_Avoid_: 主图

**子图**(subgraph):
单个 Agent 的 ReAct 循环图:动态工具暴露 + 图级边约束 + 步数上限(业务 Agent 10)。
_Avoid_: Agent 图(在上下文清晰时可简称)

**interrupt**:
图执行挂起点:状态落库、执行协程释放(durable execution);审批决定后 resume 从断点继续,可跨进程重启。
_Avoid_: 阻塞等待(旧系统轮询审批的语义,勿混用)

**事件总线**:
进程内触发层:事件触发处理图(如报告生成 → 订单 Agent 建草稿图、订单变更 → 客服通知图)。处理逻辑图化,事件语义按新架构重设计。
_Avoid_: 消息总线(易与聊天消息混淆)

**依赖声明**:
Manager 规划时声明子任务依赖关系:独立子任务并行扇出、依赖子任务串行。
_Avoid_: 流程编排

**查证优先**:
客服 Agent 的图级硬约束:出草稿前必调 faq_search/order_lookup,禁止凭空作答。
_Avoid_: 两阶段工作流(ADR-0002 旧机制,已被取代)

## 风险控制

**三层分类**:
工具风险的静态分级——免审(draft 内部编辑)、审批(一切对外状态变更:上架/下架/改价/删除/订单流转/取消)、禁做(不暴露给 LLM)。
_Avoid_: 高危操作清单(旧语义,仅两类)

**审批批次**:
切片内高危写动作按类型打包的审批单:一次 interrupt 携带一批同类动作,人工一次决定,批内同进同退;参数快照人类可读格式化。
_Avoid_: 审批单(单数,旧逐条语义)、逐条审批

**影子模式**:
环境级配置:演练环境开影子(AI 高危建议只记录待补执行,演示 AI 自主性),生产环境锁审批,运行时不可切换。
_Avoid_: 静默模式

**分层失败语义**:
工具瞬时错误本地重试 1-2 次 → 仍败上抛 Manager 统筹(换 Agent/换路径/如实告知用户)→ 永不静默吞错;
子图 LLM 失败与工具失败同策略:切片如实产出「未完成+原因」,不触发重规划(重规划仅由人工拒绝触发)。
_Avoid_: 失败兜底(旧 BaseAgent 状态机语义)

## 数据与观测

**向量仓库接口**(VectorRepository):
向量访问的统一抽象;当前实现 Milvus Standalone(Docker),pgvector 实现可切换。
_Avoid_: 向量库直连

**汇率快照**:
订单落库时记录实时汇率(基准 CNY);汇率 API 失效且缓存为空时**留空落库**并产生 fx_missing 通知(人工可见待核),不再拒单。
_Avoid_: 实时换算(历史金额会漂移)

**Langfuse**:
自托管观测平台(trace/evals):Manager 规划、子图、工具全链路可追踪;数据不出局域网;审批决定与 trace 互链。
_Avoid_: LangSmith(已弃用)

## 前端

**三视图**:
驾驶舱(Manager+3 业务卡+切片时间线+事件流实况墙)/ 客服起草工作台(双栏:左买家消息+查询证据、右可编辑草稿+一键复制)/ 切片式审批中心(审批单携带任务上下文+后续计划预览,影子段仅演练环境显示)。
_Avoid_: 页面(单页应用三视图切换)

**切片时间线**:
任务主时间线:段状态(完成/等待审批/待执行)+ 断点审批入口 + 重规划事件;StepsTimeline 为段内工具细节。
_Avoid_: 进度条(丢失断点上下文)

**通知铃铛**:
主动通知的接收 UI:按 kind 分组(order_status 订单状态 / inventory_alert 库存告警 / fx_missing 汇率缺失,未知归「其他通知」组),未读数字徽标(0 隐藏、99+ 封顶),开面板清零,每组最近 50 条,localStorage 持久化。
_Avoid_: 推送、弹窗、按 Agent 归卡(旧系统语义,新 UI 无 Agent 卡片)

## 历史决策(已被取代,保留背景)

**全量迁移**(ADR-0001):
后端整体替换为 Python 单服务——学习练习性质已退役,定位转正为真实生产工具。

**LangGraph 化**(ADR-0001):
以 LangGraph 状态图实现 ReAct 循环——扩展为纯 LangGraph 全栈(supervisor+subgraph+interrupt+checkpointer)。

**两阶段工作流**(ADR-0002):
客服阶段式强制(阶段1 必调情感分析+FAQ)——由「查证优先硬约束」取代,情感分析降级为可选工具。

**演示冲刺**(ADR-0003):
买家前台旅程与跨 Agent 事件接线——现状由重写覆盖,买家前台维持移除。

**轻量多会话**(ADR-0004):
仅 UI 与历史隔离、LLM 不带历史——升级为短上下文+摘要;多会话 UI 语义保留。
